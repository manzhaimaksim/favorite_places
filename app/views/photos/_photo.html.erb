<div class="place-photo buffer-both">
  <%= link_to image_tag(photo.photo.thumb.url), photo.photo.url %>

  <%= t('photos.phot.creator') %> <%= render partial: 'users/user', object: photo.user %>
</div>
